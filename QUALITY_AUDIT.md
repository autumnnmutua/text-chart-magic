# 图表编辑器质量审查报告

审查日期：2026-07-14

## 审查范围

- 逐模块阅读并复核 `src/` 下 121 个 TypeScript、JavaScript、Svelte 和 CSS 文件。
- 阅读 `tests/` 下 14 个浏览器测试文件。
- 阅读 15 个构建、类型、格式、测试、容器和部署配置文件。
- 合计 150 个有效文件、22,657 行。
- 覆盖 27 类图表：流程图、类图、时序图、实体关系、状态图、思维导图、架构图、块图、C4、甘特图、Git 图、鱼骨图、看板、数据包、饼图、象限图、雷达图、需求图、桑基图、时间线、树图、矩形树图、用户旅程、维恩图、沃德利地图、XY 图和 ZenUML。

以下内容未逐行审查：

- `node_modules/`：第三方依赖，由锁文件、类型检查和测试约束。
- `.svelte-kit/`、`docs/`：框架与生产构建产物，会由当前源码重新生成。
- `.wrangler/`：Cloudflare CLI 缓存与临时状态。
- `test-results/`：浏览器测试生成的追踪、截图和报告。
- 二进制字体、PNG、ICO 等静态资源：检查引用和线上加载，不按文本逐行审查。

## 问题统计

本轮登记并处理 26 个可复现问题：P0 2 个、P1 18 个、P2 6 个。P0 和已确认的 P1 均已修复并纳入自动测试。

### P0

1. 重置快照未完整覆盖源码、颜色、位置和新增元素，可能恢复到错误图表或残留用户数据。
2. 不支持的图表仍走通用分支回退，可能写入不属于该语法的内容并使整图无法渲染。

### P1

1. 不同图表的分支 ID、父级和容器归属不一致。
2. 架构图反向关系查找会交换起点和终点。
3. C4 新节点追加到文件末尾，没有真正进入所选 Boundary。
4. Wardley 中文显示名被当作组件标识，部分输入会使渲染器失效。
5. 时序图未显式声明的参与者不能进入统一文本编辑和分支逻辑。
6. 同名节点仅按文字定位，双击时可能编辑另一个节点。
7. 自主箭头缺少可持久化的数据模型，视觉线条与保存状态割裂。
8. 箭头端点不能独立重连、自由悬空或吸附到稳定锚点。
9. 箭头起终点、方向、命中层和选择状态存在竞态。
10. 模块移动后会全量重算连线，且部分箭头仍停在旧位置。
11. C4、块图和架构图节点拖拽会与 svg-pan-zoom 抢占同一手势。
12. 1280x720 高度下左侧面板会把代码编辑器压缩到约 4px。
13. 图上文本输入框先显示、下一帧才聚焦，快速确认可能落到全局快捷键。
14. 触控阻止滚动的监听器清理目标可能与注册目标不一致。
15. 拖拽和调色高频阶段可能逐像素写历史或本地存储。
16. 全局搜索在每个结果上重复扫描完整源码。
17. Alpha 同时应用到颜色值和元素透明度，透明效果被计算两次。
18. 图表重渲染后的自动居中可能覆盖用户已经调整的缩放和平移。

### P2

1. 原始示例对部分专业图表的核心语义展示不足。
2. 投资人架构示例使用实验语法，展示稳定性和可编辑性不足。
3. 自由块图示例缺少真实空位，模块和箭头容易拥挤或裁切。
4. 树图夜间主题的文字、节点和连线对比不足。
5. 饼图与象限图新增元素后缺少与其数据语义匹配的说明。
6. 短屏桌面、手机底部面板和触控目标的布局一致性不足。

## 主要修复

- 以图表策略统一分支创建、ID、父级、删除和文本字段更新；不支持的语法明确禁用，不再写入危险回退。
- 为架构图、C4、块图建立可保存的自由位置层，并在拖拽时只刷新相关连线。
- 建立持久化 `visualConnections` 模型，支持自由端、单端连接、双端连接、八点锚定、重连、反向、单向、双向、普通连线、文字、样式、删除和完整历史。
- 桌面吸附阈值为 14px，触控阈值为 18px；当前锚点在 1.35 倍阈值内保留，只有新候选至少再近 0.25 倍阈值时才切换，避免抖动。
- 拖拽使用 `requestAnimationFrame` 预览，松手后一次提交位置和历史；调色同样只在结束时持久化。
- 连接几何和并行线偏移在交互开始时缓存，200 节点场景只重算相邻连线。
- 将文本编辑、搜索替换、图层大纲、快捷键、批量选择、颜色和历史连接到同一状态模型。
- 修正 720p 左栏布局，让代码编辑器保留最小高度，其他面板在左栏内部滚动。
- 新建六份与普通作品使用相同状态结构的完成体：SaaS 产品系统架构、创业产品路线图、用户增长旅程、C4 系统关系图、市场机会矩阵、AI 产品工作流。

## 性能验证

同一台机器、Chromium、单工作进程的第二轮结果：

| 规模       | 首次渲染 | 事务式拖拽 |
| ---------- | -------: | ---------: |
| 20 个模块  |  1,238ms |      262ms |
| 100 个模块 |  1,580ms |      300ms |
| 200 个模块 |  2,009ms |      346ms |

200 个模块与 199 条自主箭头：首次渲染 2,028ms，节点拖拽 411ms，端点重连 890ms。调色连续拖动只产生 1 次持久化写入。

本轮没有保存一份与当前硬件、浏览器和测试数据完全相同的改动前基准，因此不报告虚构的百分比提升；可核验的改进是高频路径已从全量状态提交改为局部预览和结束时单次事务提交。

## 验证结果

- 分支与核心交互专项：55/55 通过。
- 全站浏览器回归：100 通过、3 条外部 GitHub 网络用例按配置跳过、0 失败。
- 单元测试：20 个测试文件、214 条测试全部通过。
- 类型检查：0 错误、0 警告。
- 静态扫描：无 `debugger`、`console.debug`、`@ts-ignore`；迁移、统计和 Service Worker 的既有运行日志保留。

## 剩余风险

- `View.svelte` 仍是职责较多的大型交互组件。当前改动保持兼容，没有在发布前进行高风险拆分；后续应按选择、文本、拖拽和连线分阶段提取控制器。
- Git、甘特、时序、ZenUML 等专业图表继续使用 Mermaid 原生连线语义，自主箭头不会接管其内部路由；这是避免破坏语义的设计限制。
- 200 节点可用，但端点重连接近 1 秒；500 节点以上仍需空间索引、虚拟化和 Web Worker 才适合持续编辑。
- 三条 GitHub gist/raw 用例需要外部网络和稳定测试资源，本地全量回归中按项目配置跳过。
- 自动测试不能替代所有真实手机浏览器差异，仍建议在一台 iPhone 和一台 Android 真机上复核长按、软键盘和双指缩放。
